# صور الموقع بدون Firebase Storage

هذه النسخة لا تستخدم Firebase Storage ولا تحتاج إلى ترقية خطة Firebase.

## عند إضافة رواية أو شخصية
اكتب **رابط الصورة المباشر** في خانة رابط الصورة، مثل:
`https://example.com/my-image.jpg`

يجب أن يكون الرابط يبدأ بـ `https://` أو `http://` وأن يفتح الصورة نفسها في المتصفح.

## ملاحظة مهمة
Firestore وAuthentication يظلان مستخدمين لحفظ البيانات وتسجيل دخول لوحة الأدمن.
صور الغلاف والشخصيات تُحفظ كرابط فقط، لذلك يجب أن تكون الصورة مستضافة على الإنترنت.
